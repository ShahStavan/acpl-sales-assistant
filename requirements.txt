# Convenience mirror of [project.dependencies] in pyproject.toml (source of truth).
# Preferred: pip install -e ".[dev]"

# --- service ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.9.0
pydantic-settings>=2.6.0

# --- data preparation & query ---
pandas>=2.2.0
duckdb>=1.1.0
openpyxl>=3.1.0          # action_playbook.xlsx
python-docx>=1.1.0       # documents/*.docx

# --- entity resolution / reconciliation ---
rapidfuzz>=3.10.0

# --- LLM provider (own key at runtime; see .env.example) ---
httpx>=0.27.0
python-dotenv>=1.0.0

# --- evaluation ---
pyyaml>=6.0.0
pytest>=8.3.0
